/** Minimal `expo-constants`, enough for `api/config.ts` to resolve a base URL. */
const Constants = {
  expoConfig: { hostUri: 'localhost:8081', version: '1.0.0' },
  expoGoConfig: { debuggerHost: 'localhost:8081' },
};

export default Constants;
